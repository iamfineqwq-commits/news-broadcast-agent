# 📝 更新日志 (Changelog)

本项目所有显著变更都记录在此文件中。
格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

---

## [0.2.0] - 2026-08-20

### ✨ 新增

#### 新闻重要性评分（有据可依的推送）

- 采集工作流（`自动获取新闻流.json`）在 LLM 摘要环节新增**重要性评分**：每条新闻按 1-5 分打分，并附评分理由
  - `5` = 行业级重大事件（头部公司重磅发布、颠覆性技术、重大政策）
  - `4` = 重要进展（知名产品更新、较大金额投融资）
  - `3` = 一般动态（常规产品迭代、一般性报道）
  - `2` = 边缘资讯（传闻、未证实消息、垂直领域动态）
  - `1` = 低价值（标题党、软文、营销内容）
- Structured Output Parser 的 JSON Schema 新增 `重要性`、`重要性理由` 两个字段
- 推送工作流（`My workflow.json`）的 AI Agent 提示词同步升级：
  - 所有新闻先按重要性**全局排序**
  - **重要性 ≥ 4 分的置顶为「今日重点」**，完整展示（标题 / 日期 / 媒体 / 摘要 / ⭐分数及理由 / 链接）
  - 重要性 < 4 分的新闻**按领域分组精简展示**（一行一条：标题 + 媒体 + 分数 + 一句话摘要 + 链接）
  - 综合建议环节优先引用重要性评分及理由进行阅读推荐
  - 对缺少评分/领域字段的旧数据做兜底处理，不会因字段缺失报错

#### 新闻领域分类（选择性阅读）

- 采集工作流新增 `领域` 字段，采用 **7 个固定枚举分类**（禁止模型自创分类，保证统计口径一致）：
  - `大模型发布` / `产品与应用` / `芯片与硬件` / `投融资` / `政策与监管` / `学术研究` / `行业动态`
- 早报卡片按领域分区渲染，读者可快速定位感兴趣的板块

### 🔧 变更

- 采集工作流的字段映射节点（Edit Fields）新增三个字段提取，并对类型做了字符串兜底（`String(... || '')`），防止模型输出数字类型导致写入飞书多维表格失败
- 写入飞书多维表格（CreateRecord）的请求体同步携带三个新字段

### ⚠️ 升级须知（Breaking / Manual Steps）

- **必须先在飞书多维表格中手动新增 3 列**，否则采集工作流写入时会报字段不存在错误：

  | 列名 | 建议类型 |
  |------|---------|
  | 重要性 | 文本（存 1-5 的数字字符串） |
  | 重要性理由 | 文本 |
  | 领域 | 单选（预先录入 7 个分类选项）或文本 |

- 需要在 n8n 中删除旧版工作流并重新导入两个新版 JSON，导入后重新绑定 DeepSeek 与 Lark 凭据

### 🗺️ 后续计划（Roadmap）

- [ ] 多源新闻去重：同一事件被多个媒体重复报道时合并展示，并将「多源共现」作为重要性加权信号
- [ ] 反馈闭环（表单轻量版）：卡片尾部附多维表格表单链接，收集「今日最有用新闻」投票，用于校准重要性评分
- [ ] 反馈闭环（完整版）：升级为飞书自建应用机器人，支持卡片按钮交互与回调

---

## [0.1.0] - 2026-08-08

### 🎉 初始发布

- **自动获取新闻流**（采集工作流）：
  - 每天 7:00 / 21:00 两次定时触发，并行抓取 4 个 AI 新闻 RSS 源（新智元 / AIBase / 腾讯科技 / 量子位）
  - 字段统一映射（标题 / 日期 / 内容 / 链接 / 媒体）、日期格式标准化、仅保留当日新闻
  - DeepSeek 逐条清洗并生成摘要（结构化 JSON 输出）
  - 写入飞书多维表格，作为新闻仓库
- **推送新闻 Bot**（早报工作流）：
  - 每天 8:00 定时触发，AI Agent 通过工具自主查询飞书多维表格（近 2 天新闻）与飞书日历（未来 7 天日程）
  - 按固定模板生成 Markdown 早报：今日 AI 新闻看板 / 近期日程概览 / 综合建议与排期参考
  - 综合建议按日程忙闲 + 新闻价值分 4 档输出（专注工作 / 推荐阅读 / 建议放松 / 具体放松活动推荐）
  - 通过飞书群自定义机器人 Webhook 推送「AI News」互动卡片
